name: Build

on:
    workflow_dispatch:
    pull_request:
        types:
            - opened
            - reopened
            - synchronize
            - labeled

jobs:
    build:
        uses: nordicsemi/pc-nrfconnect-shared/.github/workflows/build-app.yml@main
